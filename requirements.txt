# Core drafting phase
openmc
numpy

# Iteration phase (pip-installable)
montepy

# MCNPy is NOT listed here — it isn't on PyPI and needs Java 8 + MetaPy.
# See CLAUDE.md "MCNPy install notes" before running src/translate_to_mcnp.py.
